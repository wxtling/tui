import { PageModuleTypes } from '../types/index';

const modules: PageModuleTypes[] = [
  {
    path: 'crop',
    name: 'cropImg',
    navigationStyle: 'custom',
    navigationBarTitleText: '图片裁剪',
    navigationBarTextStyle: 'white',
    disableScroll: true,
  },
];

export default modules;
